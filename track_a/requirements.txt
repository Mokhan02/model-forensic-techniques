# Track A additions on top of the main repo's venv (transformers/torch/etc.
# already installed via scripts/lambda_setup.sh). Install into the SAME venv:
#   source .venv/bin/activate && pip install -r track_a/requirements.txt
anthropic>=0.40
# NOTE: a known httpx<0.28 vs anthropic-sdk-python `proxies=` incompatibility
# exists (github.com/anthropics/anthropic-sdk-python/issues/766) but pinning
# httpx<0.28 did NOT fix the APIConnectionError seen on this box's anthropic
# version, and DOES conflict with google-genai (requires httpx>=0.28.1) — so
# it's not (solely) this bug here. Do not re-pin until live_check.py's full
# traceback identifies the actual cause.
openai>=1.50
google-genai>=0.3
bitsandbytes>=0.43   # only needed for --load-in-8bit on GPUs too small for full bf16
torchvision          # AutoProcessor needs this for Muse Glimmer's image processor,
                     # even for text-only use (see roster_check.md)

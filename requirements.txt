# Core — pinned loosely; lock exact versions on the Lambda box after first install
torch>=2.4
transformers>=4.51,<5     # Qwen3 support landed in 4.51; v5 refactored TrainingArguments
accelerate>=0.34
peft>=0.13
datasets>=3.0
bitsandbytes>=0.44        # only needed for the qlora config path
sentencepiece
safetensors
pyyaml
tqdm

# Validation / analysis
numpy
pandas
scikit-learn             # probe training in Step 3
matplotlib

# Optional inference speedups for rollout collection (Step 2+)
# vllm>=0.6.3
